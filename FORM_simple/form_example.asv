

addpath('C:\Users\akselfe\OneDrive - NTNU\DERS\matlab\LongTermResponse');

import CODES.*


RSM = load('RSmodel');

TT = @(x) transform_variables_x_to_u( x,RSM );

TTinv = @(x) transform_variables_u_to_x( u,RSM );


xini = [15, 3, 2, 0.5];

u = TT(xini);

x = TTinv(u);

g = @(x) x(4) -1